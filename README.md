# service-docker-registry
experimenting with a docker registry
`scripts/services/start` to spin up the two docker images.
visit: http://localhost:8080/ for the web interface.
visit: http://localhost:5000/ for the registry endpoint.